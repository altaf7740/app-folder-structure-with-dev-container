{
    "name": "App Development Container",
    "dockerFile": "Dockerfile",
    "context": "..",
    "appPort": [
        8000
    ],
    "postCreateCommand": "uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload",
    "remoteUser": "dev"
}